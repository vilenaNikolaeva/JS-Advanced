(function solution() {
    let list = {};
    let obj = {
        add: function (el) {
            return list.add(el);
        },
        remove: function (index) {
            return list.remove(indexOf(Number(index)));
        },
        get: function (index) {
            const getIndex=list.indexOf(Number(index));
        }

    }
})();